/**
 * Конфигурация: URL бэкенда для API-запросов.
 *
 * Если фронтенд и бэкенд на одном домене (запуск локально / LAN) —
 * оставьте пустую строку: запросы пойдут на текущий origin.
 *
 * Если фронтенд на Firebase, а бэкенд отдельно —
 * укажите полный адрес, например "https://xxx.trycloudflare.com".
 */
window.API_BASE = "";
